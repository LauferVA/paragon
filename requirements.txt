# =============================================================================
# PARAGON DEPENDENCIES (v2.2.0) - AUDITED 2025-12-07
# =============================================================================
# Core runtime dependencies for the Rust-accelerated graph platform.
# Install with: pip install -r requirements.txt
# =============================================================================

# -----------------------------------------------------------------------------
# CORE: Graph & Data (Rust-backed)
# -----------------------------------------------------------------------------
rustworkx>=0.17.0          # Rust graph library (replaces NetworkX)
polars>=0.20.0             # Rust DataFrame library (replaces Pandas)
msgspec>=0.18.0            # Fast msgpack/JSON serialization (replaces Pydantic)

# -----------------------------------------------------------------------------
# CODE ANALYSIS
# -----------------------------------------------------------------------------
tree-sitter>=0.20.0        # Incremental parsing library
tree-sitter-python         # Python grammar for tree-sitter
tree-sitter-typescript     # TypeScript/TSX grammar for tree-sitter

# -----------------------------------------------------------------------------
# GRAPH ALIGNMENT
# -----------------------------------------------------------------------------
pygmtools>=0.4.0           # Graph matching algorithms (RRWM, etc.)

# -----------------------------------------------------------------------------
# SEMANTIC EMBEDDINGS (Hybrid Context Assembly)
# -----------------------------------------------------------------------------
sentence-transformers>=2.2.0  # Local embedding model (all-MiniLM-L6-v2, 384 dims)

# -----------------------------------------------------------------------------
# LLM INFRASTRUCTURE
# -----------------------------------------------------------------------------
litellm>=1.0.0             # Multi-provider LLM abstraction (OpenAI, Anthropic, Ollama)
tenacity>=8.0.0            # Retry logic with exponential backoff

# -----------------------------------------------------------------------------
# ORCHESTRATION
# -----------------------------------------------------------------------------
langgraph>=0.2.0           # LangChain graph orchestration (stable API)
langgraph-checkpoint-sqlite>=1.0.0  # Durable SQLite checkpointing for restarts

# -----------------------------------------------------------------------------
# WEB RESEARCH
# -----------------------------------------------------------------------------
tavily-python>=0.3.0       # Industrial-grade web search for agents

# -----------------------------------------------------------------------------
# SYSTEM MONITORING
# -----------------------------------------------------------------------------
psutil>=5.9.0              # System resource monitoring (CPU, RAM)

# -----------------------------------------------------------------------------
# SPEECH-TO-TEXT (Voice Input)
# -----------------------------------------------------------------------------
faster-whisper>=1.0.0      # Fast Whisper inference via CTranslate2 (4x speedup)
numpy>=1.24.0              # Array processing for audio data

# -----------------------------------------------------------------------------
# DATA GENERATION (Forge Module)
# -----------------------------------------------------------------------------
faker>=20.0.0              # Realistic synthetic data generation for domain masking
scipy>=1.11.0              # Statistical distributions and correlations

# -----------------------------------------------------------------------------
# WEB SERVER & API
# -----------------------------------------------------------------------------
granian>=0.5.0             # Rust-based ASGI server
starlette>=0.28.0          # ASGI web framework for API routes

# -----------------------------------------------------------------------------
# CONFIGURATION
# -----------------------------------------------------------------------------
PyYAML>=6.0.0              # YAML parsing for agent configuration

# -----------------------------------------------------------------------------
# OBSERVABILITY
# -----------------------------------------------------------------------------
rerun-sdk>=0.18.0          # Visual debugging and temporal replay

# -----------------------------------------------------------------------------
# OPTIONAL: Development & Testing
# -----------------------------------------------------------------------------
# pytest>=8.0.0            # Testing framework
# hypothesis>=6.0.0        # Property-based testing
# ruff>=0.1.0              # Fast linter

# -----------------------------------------------------------------------------
# OPTIONAL: LLM Providers (install as needed)
# -----------------------------------------------------------------------------
# anthropic>=0.18.0        # Anthropic Claude SDK
# openai>=1.0.0            # OpenAI SDK

# =============================================================================
# ENVIRONMENT VARIABLES (Required/Recommended)
# =============================================================================
# Set these in your shell profile (.bashrc, .zshrc) or .env file:
#
# REQUIRED for Researcher module:
#   TAVILY_API_KEY          - Tavily web search API key (https://tavily.com)
#
# REQUIRED for LLM features:
#   ANTHROPIC_API_KEY       - Anthropic Claude API key (https://console.anthropic.com)
#   OR
#   OPENAI_API_KEY          - OpenAI API key (https://platform.openai.com)
#
# OPTIONAL for local LLM routing:
#   OLLAMA_HOST             - Ollama server URL (default: http://localhost:11434)
#
# =============================================================================
